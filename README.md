# Merge Commit

![Performing a Merge Commit](merge-commit.png)

## Comandos

```bash
git checkout main 
git merge featureX --no-ff
git branch -d featureX
git push origin main
git push origin --delete featureX
```